import { type Dictionary, t } from "intlayer";

const siteSwitcherContent = {
	key: "site-switcher",
	content: {
		addSite: t({
			en: "Add Site",
			es: "Agregar Sitio",
		}),
	},
} satisfies Dictionary;

export default siteSwitcherContent;
