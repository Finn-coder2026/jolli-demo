# E2E Test Environment Variables
# Copy this file to .env.e2e and fill in the values

# =============================================================================
# User Configuration (required for token generation)
# =============================================================================
# To find your user ID, login once via Google, then query the database:
#   SELECT id, email, name FROM users;

E2E_USER_ID=1
E2E_ORG_ID=your org ID from database
E2E_TENANT_ID=your tenant ID from database
E2E_USER_EMAIL=your-email@example.com
E2E_USER_NAME=Your Name

# =============================================================================
# Generated Token (output of: npm run e2e:token --workspaces=false)
# =============================================================================
# After running the token generation script, paste the output here:

E2E_TEST_TOKEN=your-generated-token-here

# =============================================================================
# Optional Configuration
# =============================================================================

# Override the JWT signing secret (default: 'dev', must match backend/.env)
# TOKEN_SECRET=dev

# Override the base URL (default: https://main.jolli-local.me)
# E2E_BASE_URL=https://main.jolli-local.me
